{
  "name": "tubesstima2",
  "private": true,
  "type": "module",
  "workspaces": [
    "src/backend",
    "src/frontend"
  ],
  "scripts": {
	"dev": "bun run --parallel --filter frontend --filter backend dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}